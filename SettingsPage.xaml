<Page
    x:Class="PowerMonitorApp.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerMonitorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Padding="24" Spacing="24">
        <TextBlock Text="Settings" Style="{ThemeResource TitleTextBlockStyle}" />

        <StackPanel Spacing="8">
            <TextBlock Text="Auto Update" Style="{ThemeResource SubtitleTextBlockStyle}" />
            <ToggleSwitch IsOn="{x:Bind ViewModel.AutoUpdate, Mode=TwoWay}" Header="Automatically check for updates on startup" />
            <Button Content="Check for Updates Now" Click="CheckUpdates_Click" Margin="0,8,0,0"/>
        </StackPanel>
    </StackPanel>
</Page>
